%ifndef MULTIBOOT_ASM
%define MULTIBOOT_ASM

BITS 32                             ; use 32-bit Protected Mode

%define MULTIBOOT_MAGIC 0x1BADB002   ; Multiboot magic number

; Multiboot header

%endif ; MULTIBOOT_ASM